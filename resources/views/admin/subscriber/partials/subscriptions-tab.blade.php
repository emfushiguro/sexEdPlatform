{{-- Subscriptions Tab: delegates to unified index --}}
<div class="py-2 px-1 text-sm text-gray-500 dark:text-gray-400">
    {{-- This partial is kept for backward compatibility. The subscriber list is now rendered by admin.subscriber.index. --}}
    <p class="text-center py-8">Redirecting to <a href="{{ route('admin.subscribers.index') }}" class="text-brand-500 underline">subscriber management</a>.</p>
</div>
